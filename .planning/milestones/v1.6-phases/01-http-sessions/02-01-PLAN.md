---
phase: 02-connection-pooling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/db/engine.py
  - customer_portal_api/app/db.py
  - core/lifecycle.py
autonomous: true
requirements: [POOL-01]

must_haves:
  truths:
    - "Database connections are recycled after 3600 seconds to prevent stale connection errors"
    - "Up to 30 concurrent database connections can be served (20 base + 10 overflow)"
    - "Stale connections are detected before use via pool_pre_ping"
    - "Engine pool is cleanly disposed on application shutdown"
  artifacts:
    - core/db/engine.py with QueuePool configuration
    - customer_portal_api/app/db.py with matching pool config
    - engine.dispose() call in lifecycle shutdown
  key_links:
    - "engine.py create_engine → QueuePool with production defaults"
    - "lifecycle.py stop() → engine.dispose()"
---

<objective>
Configure SQLAlchemy connection pooling with production-ready defaults for both the main
application engine and the customer portal engine. Add graceful pool disposal on shutdown.

Purpose: The current bare `create_engine(url)` creates a new connection per Session with no
reuse, no stale connection detection, and no clean shutdown. Under concurrent load (Phase 3
parallel registration), this will exhaust PostgreSQL connections.

Output: Configured engines with QueuePool, pool_pre_ping enabled, and clean shutdown behavior.
</objective>

<execution_context>
@/home/vitaly/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/vitaly/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-http-sessions/02-CONTEXT.md
@core/db/engine.py
@customer_portal_api/app/db.py
@core/lifecycle.py
@main.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Configure QueuePool on both database engines</name>
  <files>core/db/engine.py, customer_portal_api/app/db.py</files>
  <read_first>
    - core/db/engine.py (current bare create_engine call)
    - customer_portal_api/app/db.py (current bare create_engine call)
  </read_first>
  <behavior>
    - Test: engine.pool.status() returns a string containing "Pool size" after engine creation
    - Test: engine.pool.size() == 20 (default pool_size per D-01)
    - Test: engine.pool.overflow() == 0 initially (no overflow connections yet)
    - Test: engine._kwargs contains pool_recycle=3600
    - Test: engine._kwargs contains pool_pre_ping=True
    - Test: customer_portal engine has identical pool settings
  </behavior>
  <action>
    In core/db/engine.py:

    1. Add imports at top: `from sqlalchemy.pool import QueuePool`

    2. Replace the bare `create_engine(url)` call in `_create_sync_engine` (line 53) with:
       `create_engine(url, poolclass=QueuePool, pool_size=20, max_overflow=10, pool_recycle=3600, pool_pre_ping=True)`
       This implements D-01 (QueuePool with production defaults).

    3. For SQLite URLs, keep NullPool behavior — add a conditional: if url starts with "sqlite://",
       skip pool kwargs (SQLite doesn't support connection pooling the same way). Use:
       `if url.startswith("sqlite+"): create_engine(url)` else `create_engine(url, poolclass=QueuePool, ...)`.

    In customer_portal_api/app/db.py:

    4. Add `from sqlalchemy.pool import QueuePool` import.

    5. Update the `create_engine` call (line 21) with identical pool kwargs:
       `poolclass=QueuePool, pool_size=20, max_overflow=10, pool_recycle=3600, pool_pre_ping=True`.
       Keep the existing `connect_args={"check_same_thread": False}` for SQLite conditional.
  </action>
  <verify>
    <automated>cd /home/vitaly/projects/any-auto-register && python3 -c "
from core.db.engine import engine
from sqlalchemy.pool import QueuePool
assert isinstance(engine.pool, QueuePool), f'Expected QueuePool, got {type(engine.pool)}'
assert engine.pool.size() == 20, f'Expected pool_size=20, got {engine.pool.size()}'
assert engine.pool._recycle == 3600, f'Expected pool_recycle=3600, got {engine.pool._recycle}'
print('OK: main engine pool configured')
"</automated>
  </verify>
  <done>
    - core/db/engine.py creates engine with QueuePool, pool_size=20, max_overflow=10, pool_recycle=3600, pool_pre_ping=True
    - customer_portal_api/app/db.py creates engine with identical pool settings
    - SQLite URLs skip pool kwargs (NullPool default)
    - All 94 existing Session(engine) call sites work unchanged
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add engine.dispose() on application shutdown</name>
  <files>core/lifecycle.py, main.py</files>
  <read_first>
    - core/lifecycle.py (LifecycleManager.stop method at line 484)
    - main.py (lifespan shutdown sequence at line 75-83)
  </read_first>
  <behavior>
    - Test: After LifecycleManager.stop() returns, engine.pool.status() contains "Closed" or engine is disposed
    - Test: main.py lifespan yield block calls engine.dispose() after stopping lifecycle_manager
  </behavior>
  <action>
    In core/lifecycle.py:

    1. Add import: `from core.db import engine as _db_engine`

    2. In LifecycleManager.stop() method (line 484), after `self._thread.join(timeout=5)`, add:
       `_db_engine.dispose()`
       This cleanly closes all pooled connections on shutdown.

    In main.py:

    3. In the lifespan shutdown block (after yield, line 75-83), add at the end:
       `from core.db import engine as _engine; _engine.dispose()`
       This ensures the pool is disposed even if lifecycle_manager was never started.

    Note: Both dispose calls are idempotent — SQLAlchemy's dispose() is safe to call multiple times.
  </action>
  <verify>
    <automated>cd /home/vitaly/projects/any-auto-register && python3 -c "
from core.db.engine import engine
# Verify dispose is callable (pool is open)
engine.dispose()
# After dispose, pool should be closed
status = engine.pool.status()
print(f'Pool status after dispose: {status}')
print('OK: engine.dispose() works')
"</automated>
  </verify>
  <done>
    - engine.dispose() called in LifecycleManager.stop()
    - engine.dispose() called in main.py lifespan shutdown block
    - dispose is idempotent (safe to call multiple times)
    - No existing behavior changed
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Application → PostgreSQL | Connection pool manages reuse of DB connections |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Denial of Service | DB pool exhaustion | high | mitigate | pool_size=20, max_overflow=10 limits total connections to 30 |
| T-02-02 | Information Disclosure | Stale connection data leak | medium | mitigate | pool_pre_ping=True detects stale connections before use |
| T-02-03 | Elevation of Privilege | Connection leak on error | medium | mitigate | Session context manager ensures connections return to pool |
| T-02-SC | Tampering | npm/pip/cargo installs | low | accept | No new dependencies in this plan |
</threat_model>

<verification>
1. `python3 -c "from core.db.engine import engine; print(engine.pool.status())"` shows QueuePool config
2. `python3 -c "from core.db.engine import engine; engine.dispose(); print('disposed')"` succeeds
3. All existing tests pass: `python3 -m pytest tests/ -x --tb=short -q` (if tests exist)
</verification>

<success_criteria>
- Database engine uses QueuePool with pool_size=20, max_overflow=10, pool_recycle=3600, pool_pre_ping=True
- Customer portal engine has identical pool configuration
- engine.dispose() called on both shutdown paths (LifecycleManager.stop + main.py lifespan)
- Zero breaking changes to 94 existing Session(engine) call sites
</success_criteria>

<output>
Create `.planning/phases/01-http-sessions/02-01-SUMMARY.md` when done
</output>
