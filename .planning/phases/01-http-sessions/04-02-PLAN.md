---
phase: 04-rate-limiting
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - infrastructure/health_runtime.py
  - application/health.py
  - api/health.py
autonomous: true
requirements: [RATE-04]

must_haves:
  truths:
    - "GET /api/rate-limits endpoint returns current rate limit metrics"
    - "Rate limit metrics include hits, current usage, and throttle events per platform/provider"
    - "HealthRuntime exposes rate_limit_status method aggregating all limiter metrics"
  artifacts:
    - infrastructure/health_runtime.py
    - application/health.py
    - api/health.py
  key_links:
    - "HealthRuntime.rate_limit_status() calls rate_limit_metrics.get_metrics() from core/rate_limiter.py"
    - "HealthService.rate_limit_status() delegates to HealthRuntime"
    - "GET /api/rate-limits returns the same dict as rate_limit_status()"
---

<objective>
Expose rate limit metrics via the health API: add rate_limit_status to HealthRuntime, expose through HealthService, and create GET /api/rate-limits endpoint.

Purpose: Make rate limit observability available to operators via the existing health API pattern.
Output: Modified infrastructure/health_runtime.py, application/health.py, api/health.py
</objective>

<execution_context>
@/home/vitaly/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/vitaly/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-http-sessions/04-CONTEXT.md

# Plan 1 created this
@.planning/phases/01-http-sessions/04-01-SUMMARY.md

# Existing health infrastructure to extend
@infrastructure/health_runtime.py
@application/health.py
@api/health.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add rate_limit_status to HealthRuntime and HealthService</name>
  <files>infrastructure/health_runtime.py, application/health.py</files>

  <read_first>
    - infrastructure/health_runtime.py — Current HealthRuntime class with health(), pool_status(), readiness() methods
    - application/health.py — Current HealthService delegating to HealthRuntime
    - core/rate_limiter.py — rate_limit_metrics singleton and RateLimitMetrics.get_metrics() (created in Plan 1)
  </read_first>

  <action>
1. **infrastructure/health_runtime.py** — Add `rate_limit_status` method to `HealthRuntime`:
   - Import `rate_limit_metrics` from `core.rate_limiter` at top of file
   - Add method `rate_limit_status(self) -> dict` that returns `rate_limit_metrics.get_metrics()`
   - Also include a summary section with `_platform_limiters` and `_provider_limiters` counts from `core.rate_limiter` module-level dicts (import `_platform_limiters, _provider_limiters` from `core.rate_limiter`)
   - Return format: `{"metrics": rate_limit_metrics.get_metrics(), "active_platform_limiters": len(_platform_limiters), "active_provider_limiters": len(_provider_limiters)}`

2. **application/health.py** — Add `rate_limit_status` method to `HealthService`:
   - Add method `rate_limit_status(self) -> dict` that delegates to `self.runtime.rate_limit_status()`

   Follow the exact delegation pattern used by `pool_status()` (lines 16-17 of application/health.py).
  </action>

  <verify>
    <automated>cd /home/vitaly/projects/any-auto-register && python -c "
from infrastructure.health_runtime import HealthRuntime
from application.health import HealthService

# Test HealthRuntime
runtime = HealthRuntime()
status = runtime.rate_limit_status()
assert 'metrics' in status
assert 'active_platform_limiters' in status
assert 'active_provider_limiters' in status
assert 'hits' in status['metrics']
assert 'usage' in status['metrics']
assert 'throttles' in status['metrics']

# Test HealthService delegation
service = HealthService()
service_status = service.rate_limit_status()
assert service_status == status  # same data

print('HealthRuntime and HealthService rate_limit_status tests passed')
"</automated>
  </verify>

  <acceptance_criteria>
    - HealthRuntime has rate_limit_status() method returning metrics dict with hits/usage/throttles
    - HealthRuntime.rate_limit_status() includes active_platform_limiters and active_provider_limiters counts
    - HealthService.rate_limit_status() delegates to HealthRuntime.rate_limit_status()
    - Import of rate_limit_metrics from core.rate_limiter succeeds
    - Both methods return dict with expected keys
  </acceptance_criteria>

  <done>
    HealthRuntime.rate_limit_status() returns rate limit metrics from the singleton.
    HealthService.rate_limit_status() delegates correctly.
    Rate limit observability is available through the health service layer.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add GET /api/rate-limits endpoint</name>
  <files>api/health.py</files>

  <read_first>
    - api/health.py — Current router with /health, /ready, /pools endpoints
    - application/health.py — HealthService with rate_limit_status() (added in Task 1)
  </read_first>

  <action>
Add a new endpoint to `api/health.py`:

```python
@router.get("/rate-limits")
def rate_limits():
    """Rate limit metrics for monitoring."""
    return service.rate_limit_status()
```

Follow the exact pattern of the existing `/pools` endpoint (line 22-24 of api/health.py). The `service` variable is already module-level (line 8). Add the new endpoint after the existing `/pools` endpoint.
  </action>

  <verify>
    <automated>cd /home/vitaly/projects/any-auto-register && python -c "
from api.health import router
from fastapi.testclient import TestClient

client = TestClient(router)
response = client.get('/rate-limits')
assert response.status_code == 200
data = response.json()
assert 'metrics' in data
assert 'active_platform_limiters' in data
assert 'active_provider_limiters' in data
print('GET /api/rate-limits endpoint test passed')
print('Response:', data)
"</automated>
  </verify>

  <acceptance_criteria>
    - GET /api/rate-limits endpoint returns 200
    - Response JSON contains "metrics" key with hits/usage/throttles
    - Response JSON contains "active_platform_limiters" and "active_provider_limiters" keys
    - Endpoint follows existing /pools endpoint pattern
    - No authentication required (consistent with /health and /pools)
  </acceptance_criteria>

  <done>
    GET /api/rate-limits endpoint returns current rate limit metrics.
    Response includes hits, usage, throttles per platform/provider, and active limiter counts.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External client → /api/rate-limits | Metrics endpoint exposed without auth (same as /health, /pools) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4-04 | Information Disclosure | /api/rate-limits endpoint | low | accept | Metrics contain only aggregate counts/timestamps, no user data or secrets. Consistent with existing /health and /pools endpoints that also lack auth. |
| T-4-05 | Denial of Service | Rate limit metrics memory growth | low | accept | RateLimitMetrics._usage capped at 60s window with cleanup per entry. Bounded by number of unique platform/provider keys. |
</threat_model>

<verification>
- GET /api/rate-limits returns 200 with metrics JSON
- HealthRuntime.rate_limit_status() returns dict with metrics, active_platform_limiters, active_provider_limiters
- HealthService.rate_limit_status() delegates to HealthRuntime
</verification>

<success_criteria>
- GET /api/rate-limits endpoint returns rate limit metrics
- Metrics include hits, usage (60s window), and throttles per platform/provider
- Active limiter counts included in response
- Endpoint follows existing health API patterns
</success_criteria>

<output>
Create `.planning/phases/01-http-sessions/04-02-SUMMARY.md` when done
</output>
