---
phase: 03-concurrent-registration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/db/models.py
  - core/db/migrations.py
  - core/db/engine.py
  - application/tasks/task_repository.py
  - application/tasks/__init__.py
  - alembic/versions/002_add_task_priority.py
autonomous: true
requirements:
  - CONC-01
  - CONC-03
must_haves:
  truths:
    - Tasks can be created with a priority level (high, normal, low)
    - Pending tasks are claimed in priority order (high before normal before low), then by creation time
    - Priority is visible in task serialization (API response)
    - Backward compatibility: existing tasks default to normal priority
  artifacts:
    - core/db/models.py with priority field on TaskModel
    - alembic/versions/002_add_task_priority.py migration file
    - application/tasks/task_repository.py with priority-based claiming
  key_links:
    - TaskModel.priority drives claim_next_runnable_task ordering
    - create_task accepts priority parameter and stores it
    - serialize_task includes priority in output dict
---

<objective>
Add priority field to TaskModel and make task claiming priority-aware.

Purpose: Foundation for concurrent registration — tasks must be claimable in priority order so urgent registrations run before batch jobs. This is the prerequisite for Plan 2 (enhanced TaskRuntime).
Output: Database schema with priority column, priority-based claiming logic, updated serialization.
</objective>

<execution_context>
@/home/vitaly/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/vitaly/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-http-sessions/03-CONTEXT.md

Key source files:
- core/db/models.py — TaskModel definition (line 256-286), currently has no priority field
- core/db/engine.py — _ensure_column helper for safe column addition (line 100-121)
- core/db/migrations.py — existing migration pattern using SchemaVersionModel (line 65-76)
- application/tasks/task_repository.py — claim_next_runnable_task orders by created_at (line 267-271), create_task (line 289-313)
- application/tasks/task_scheduler.py — duplicate claim_next_runnable_task (line 94-122)
- alembic/versions/001_initial_schema.py — migration format reference
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add priority field to TaskModel and Alembic migration</name>
  <files>core/db/models.py, alembic/versions/002_add_task_priority.py, core/db/engine.py</files>
  <read_first>
    - core/db/models.py — see TaskModel class (line 256-286) to add priority field
    - core/db/engine.py — see _ensure_column helper (line 100-121) and _VALID_TABLES allowlist (models.py line 17-30)
    - alembic/versions/001_initial_schema.py — see migration format (revision, downgrade pattern)
    - core/db/migrations.py — see _migration_applied / _mark_migration_applied pattern (line 65-76)
  </read_first>
  <behavior>
    - TaskModel has a `priority` field of type str with default "normal"
    - Valid priority values: "high", "normal", "low"
    - Alembic migration adds `priority` column to tasks table with server_default "normal"
    - Existing tasks in database get priority "normal" after migration
    - _ensure_column in engine.py does not break if priority column already exists
  </behavior>
  <action>
    1. In core/db/models.py TaskModel class (after line 268, after `error: str = ""`), add: `priority: str = Field(default="normal", index=True)`

    2. Create alembic/versions/002_add_task_priority.py with:
       - revision = "002", down_revision = "001"
       - upgrade(): op.add_column("tasks", sa.Column("priority", sa.String(), server_default="normal")) then op.create_index("ix_tasks_priority", "tasks", ["priority"])
       - downgrade(): op.drop_index("ix_tasks_priority") then op.drop_column("tasks", "priority")

    3. In core/db/engine.py init_db() function (around line 175), add a call to _ensure_column("tasks", "priority", "TEXT DEFAULT 'normal'") — this handles SQLite where Alembic doesn't run.

    4. Add priority validation: in models.py, define PRIORITY_LEVELS = frozenset({"high", "normal", "low"}) as a module-level constant. In create_task (task_repository.py), validate priority against PRIORITY_LEVELS and default to "normal" if invalid.
  </action>
  <verify>
    <automated>cd /home/vitaly/projects/any-auto-register && python -c "from core.db.models import TaskModel, PRIORITY_LEVELS; t = TaskModel(id='test', type='register', priority='high'); assert t.priority == 'high'; assert 'high' in PRIORITY_LEVELS; assert 'normal' in PRIORITY_LEVELS; assert 'low' in PRIORITY_LEVELS; print('Model OK')" && python -c "import ast; tree = ast.parse(open('alembic/versions/002_add_task_priority.py').read()); print('Migration syntax OK')"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "priority.*Field" core/db/models.py returns 0 (field exists)
    - grep -q "ix_tasks_priority" alembic/versions/002_add_task_priority.py returns 0 (index in migration)
    - grep -q "PRIORITY_LEVELS" core/db/models.py returns 0 (constant exists)
    - python -c "from core.db.models import TaskModel; t = TaskModel(id='x', type='r', priority='high'); assert t.priority == 'high'" exits 0
    - python -c "from core.db.models import PRIORITY_LEVELS; assert PRIORITY_LEVELS == frozenset({'high', 'normal', 'low'})" exits 0
  </acceptance_criteria>
  <done>TaskModel has priority field with default "normal", Alembic migration adds column + index, _ensure_column handles SQLite, PRIORITY_LEVELS constant validates input.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Priority-based task claiming and serialization</name>
  <files>application/tasks/task_repository.py, application/tasks/task_scheduler.py, application/tasks/__init__.py</files>
  <read_first>
    - application/tasks/task_repository.py — see claim_next_runnable_task (line 258-286) ordering by created_at, create_task (line 289-313), serialize_task (line 115-144)
    - application/tasks/task_scheduler.py — see duplicate claim_next_runnable_task (line 94-122) must stay in sync
    - application/tasks/__init__.py — see exports to update
  </read_first>
  <behavior>
    - claim_next_runnable_task orders pending tasks by priority (high > normal > low) then created_at (FIFO within same priority)
    - create_task accepts optional priority parameter, validates against PRIORITY_LEVELS, defaults to "normal"
    - serialize_task includes "priority" key in output dict
    - TaskScheduler's claim_next_runnable_task uses same priority ordering (kept in sync)
    - create_register_task, create_account_check_task, create_platform_action_task pass through priority from payload if present
  </behavior>
  <action>
    1. In task_repository.py claim_next_runnable_task (line 267-271): Change the ORDER BY from `.order_by(TaskModel.created_at)` to a priority-aware ordering. Since SQL string comparison won't work for priority ordering (alphabetical: high < low < normal), use a Python-side sort after fetching all pending tasks. Replace the `session.exec(select(TaskModel).where(...).order_by(TaskModel.created_at)).all()` with fetching all pending tasks, then sorting them with a key function: `_priority_sort_key(task)` that returns `({"high": 0, "normal": 1, "low": 2}.get(task.priority, 1), task.created_at)`.

    2. Define `_priority_sort_key(task: TaskModel) -> tuple[int, datetime]` as a helper function in task_repository.py.

    3. In task_repository.py create_task (line 289-313): Add `priority: str = "normal"` parameter. Validate against PRIORITY_LEVELS, default to "normal" if invalid. Set `task.priority = priority` before session.add.

    4. In task_repository.py serialize_task (line 115-144): Add `"priority": task.priority` to the returned dict (after the `"platform"` entry).

    5. In task_scheduler.py claim_next_runnable_task (line 94-122): Apply the same priority ordering change — fetch all pending, sort by priority then created_at.

    6. In task_repository.py create_register_task (line 316-323): Extract priority from payload and pass to create_task: `priority = str(payload.get("priority", "normal") or "normal")`.

    7. In task_repository.py create_account_check_task (line 326-337): Default priority "normal" (no change needed, create_task defaults handle it).

    8. In task_repository.py create_platform_action_task (line 349-355): Extract priority from payload same as register.
  </action>
  <verify>
    <automated>cd /home/vitaly/projects/any-auto-register && python -c "
from application.tasks.task_repository import create_task, get_task, _priority_sort_key
from core.db.models import TaskModel
# Test priority sort key
from datetime import datetime
t1 = TaskModel(id='1', type='r', priority='low', created_at=datetime(2026,1,1))
t2 = TaskModel(id='2', type='r', priority='high', created_at=datetime(2026,1,2))
t3 = TaskModel(id='3', type='r', priority='normal', created_at=datetime(2026,1,1))
tasks = [t1, t2, t3]
sorted_tasks = sorted(tasks, key=_priority_sort_key)
assert [t.id for t in sorted_tasks] == ['2', '3', '1'], f'Got {[t.id for t in sorted_tasks]}'
print('Priority sort OK')
" && python -c "
from application.tasks.task_repository import serialize_task
from core.db.models import TaskModel
from datetime import datetime
t = TaskModel(id='x', type='register', platform='chatgpt', priority='high', created_at=datetime(2026,1,1))
s = serialize_task(t)
assert s['priority'] == 'high', f'Got {s.get(\"priority\")}'
print('Serialize OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "_priority_sort_key" application/tasks/task_repository.py returns 0
    - grep -q "priority.*task.priority" application/tasks/task_repository.py (in serialize_task) returns 0
    - grep -q 'priority.*payload.get' application/tasks/task_repository.py (in create_task) returns 0
    - grep -q "_priority_sort_key" application/tasks/task_scheduler.py returns 0
    - python sorting test passes: high tasks come before normal, normal before low, same priority ordered by created_at
  </acceptance_criteria>
  <done>claim_next_runnable_task orders by priority (high > normal > low) then created_at. create_task validates and stores priority. serialize_task includes priority. Both task_repository and task_scheduler claiming logic are synchronized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API input → TaskModel.priority | User-provided priority value must be validated against PRIORITY_LEVELS |
| Database → claiming logic | Priority ordering must not be bypassable via malformed data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | TaskModel.priority | medium | mitigate | Validate against PRIORITY_LEVELS frozenset in create_task; default to "normal" on invalid input |
| T-03-02 | Elevation of Privilege | claim_next_runnable_task | low | accept | Priority only affects scheduling order, not access control; no privilege escalation vector |
| T-03-SC | Tampering | alembic migration | high | mitigate | Migration is idempotent (server_default handles existing rows); downgrade reverses cleanly |
</threat_model>

<verification>
1. `python -c "from core.db.models import TaskModel, PRIORITY_LEVELS; print('Model OK')"` — imports succeed
2. `python -c "from application.tasks.task_repository import claim_next_runnable_task, create_task; print('Repo OK')"` — functions exist
3. Priority sort test: high < normal < low in sort order, same priority sorts by created_at
4. Serialization includes priority key
5. `alembic/versions/002_add_task_priority.py` exists and has valid Python syntax
</verification>

<success_criteria>
- TaskModel has `priority: str = Field(default="normal", index=True)` field
- PRIORITY_LEVELS = frozenset({"high", "normal", "low"}) exists in models.py
- claim_next_runnable_task orders pending tasks by priority descending then created_at ascending
- create_task validates and stores priority
- serialize_task includes priority in output
- Alembic migration adds priority column with server_default "normal"
- Both task_repository and task_scheduler claiming logic use same priority ordering
</success_criteria>

<output>
Create `.planning/phases/01-http-sessions/03-01-SUMMARY.md` when done
</output>
