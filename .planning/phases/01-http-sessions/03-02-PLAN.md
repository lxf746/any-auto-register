---
phase: 03-concurrent-registration
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - services/task_runtime.py
  - application/tasks/task_runner.py
  - core/db/engine.py
autonomous: true
requirements:
  - CONC-01
  - CONC-02
  - CONC-04
must_haves:
  truths:
    - TaskRuntime dispatches tasks in priority order using a priority queue
    - Resource usage (CPU, memory) is tracked and exposed via metrics
    - Dynamic concurrency adjustment reduces workers when system is under load
    - Worker pool state is visible (running count, per-platform counts, resource usage)
  artifacts:
    - services/task_runtime.py with priority queue dispatch and resource monitoring
    - application/tasks/task_runner.py with resource-aware execution limits
    - core/db/engine.py with resource metrics helper
  key_links:
    - TaskRuntime._loop uses heapq priority queue instead of simple FIFO
    - ResourceMonitor tracks CPU/memory and feeds back to available_slots calculation
    - Worker state includes resource usage data for monitoring
---

<objective>
Enhance TaskRuntime with priority queue dispatch and resource-aware concurrency limits.

Purpose: Make the task dispatcher priority-aware (high tasks run first) and resource-aware (reduce concurrency when system is under load). This completes the concurrent registration phase.
Output: Priority-queue-based dispatcher, resource monitoring, dynamic concurrency adjustment.
</objective>

<execution_context>
@/home/vitaly/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/vitaly/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-http-sessions/03-CONTEXT.md
@.planning/phases/01-http-sessions/03-01-SUMMARY.md

Key source files:
- services/task_runtime.py — TaskRuntime class (line 21-111), _loop (line 57-95), _workers dict
- application/tasks/task_runner.py — _execute_register_task uses ThreadPoolExecutor with concurrency param (line 340-520)
- core/db/engine.py — engine creation, pool config
- core/db/models.py — TaskModel with priority field (added in Plan 1)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Priority queue dispatch in TaskRuntime</name>
  <files>services/task_runtime.py</files>
  <read_first>
    - services/task_runtime.py — full file, understand _loop dispatch logic (line 57-95), _workers dict (line 28), _reap_workers (line 104-108)
    - application/tasks/task_repository.py — claim_next_runnable_task returns task dict with "id", "platform", "account_keys"; priority field added in Plan 1
  </read_first>
  <behavior>
    - TaskRuntime._loop uses a priority queue (heapq) to order pending tasks by priority before dispatching
    - Tasks with priority "high" are dispatched before "normal", "normal" before "low"
    - Within same priority, tasks are dispatched in FIFO order (by created_at)
    - Worker state tracks the priority of each running task for monitoring
    - Available slots calculation remains the same (max_parallel_tasks - running count)
  </behavior>
  <action>
    1. Import heapq at the top of services/task_runtime.py.

    2. In TaskWorkerState dataclass (line 14-18), add field: `priority: str = "normal"`.

    3. Create a helper method `_pending_task_sort_key(task_info: dict) -> tuple[int, str]` on TaskRuntime that maps priority to sort order: `{"high": 0, "normal": 1, "low": 2}.get(task_info.get("priority", "normal"), 1)` as first element, task_info["id"] as tiebreaker.

    4. In _loop method (line 57-95): Replace the inner while loop that calls claim_next_runnable_task one-at-a-time with a batch approach:
       - Collect all claimable tasks into a list by calling claim_next_runnable_task repeatedly until it returns None or slots are exhausted
       - Sort the collected tasks using _pending_task_sort_key
       - Dispatch them in sorted order (high first)
       - This requires storing the full task info including priority from claim_next_runnable_task

    5. Update claim_next_runnable_task call to also retrieve priority from the task. Since Plan 1 already returns priority in the task dict from serialize_task, verify that claim_next_runnable_task returns priority in its result dict. If it doesn't, add "priority": task.priority to the return dict in task_repository.py claim_next_runnable_task.

    6. When creating TaskWorkerState in _loop (line 84-88), pass the priority: `TaskWorkerState(thread=worker, platform=..., account_keys=..., priority=task_info.get("priority", "normal"))`.

    7. Add a method `get_runtime_stats() -> dict` that returns: running_count (len of _workers), max_parallel_tasks, per_platform_counts, per_priority_counts (count of running tasks by priority), and uptime (time since start).

    8. Store start_time in __init__: `self._started_at: float = 0.0` and set it in start(): `self._started_at = time.monotonic()`.
  </action>
  <verify>
    <automated>cd /home/vitaly/projects/any-auto-register && python -c "
import heapq
from services.task_runtime import TaskRuntime, TaskWorkerState
# Verify TaskWorkerState has priority field
w = TaskWorkerState(thread=None, priority='high')
assert w.priority == 'high'
# Verify TaskRuntime has get_runtime_stats
rt = TaskRuntime()
stats = rt.get_runtime_stats()
assert 'running_count' in stats
assert 'per_priority_counts' in stats
print('TaskRuntime priority queue OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "import heapq" services/task_runtime.py returns 0
    - grep -q "priority.*str.*normal" services/task_runtime.py (in TaskWorkerState) returns 0
    - grep -q "get_runtime_stats" services/task_runtime.py returns 0
    - grep -q "per_priority_counts" services/task_runtime.py returns 0
    - TaskWorkerState has priority field with default "normal"
    - get_runtime_stats returns dict with running_count, per_priority_counts keys
  </acceptance_criteria>
  <done>TaskRuntime uses priority queue for dispatch, worker state tracks priority, get_runtime_stats exposes running count and per-priority breakdown.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Resource-aware concurrency limits and monitoring</name>
  <files>application/tasks/task_runner.py, core/db/engine.py</files>
  <read_first>
    - application/tasks/task_runner.py — see _execute_register_task concurrency logic (line 343-344), _make_executor (line 241-242), ThreadPoolExecutor usage
    - core/db/engine.py — engine creation and pool config (line 46-66), init_db (line 163-184)
    - services/task_runtime.py — after Plan 1 changes, see TaskRuntime for integration point
  </read_first>
  <behavior>
    - ResourceMonitor tracks system CPU and memory usage
    - When CPU > 80% or memory > 85%, dynamic concurrency reduces available worker slots
    - Resource metrics are available via engine module for monitoring endpoints
    - TaskRunner's ThreadPoolExecutor concurrency is capped by resource availability
    - Pool-level metrics (DB connections) are included in resource monitoring
  </behavior>
  <action>
    1. Create a ResourceMonitor class in core/db/engine.py (or a new file core/resource_monitor.py if engine.py gets too large — prefer engine.py since it already handles pool config):

       class ResourceMonitor:
           def __init__(self, *, cpu_threshold: float = 80.0, memory_threshold: float = 85.0):
               self.cpu_threshold = cpu_threshold
               self.memory_threshold = memory_threshold

           def get_usage(self) -> dict:
               """Return current CPU % and memory usage %."""
               # Use psutil if available, else return safe defaults
               try:
                   import psutil
                   return {"cpu_percent": psutil.cpu_percent(interval=0.1), "memory_percent": psutil.virtual_memory().percent}
               except ImportError:
                   return {"cpu_percent": 0.0, "memory_percent": 0.0}

           def should_throttle(self) -> bool:
               usage = self.get_usage()
               return usage["cpu_percent"] > self.cpu_threshold or usage["memory_percent"] > self.memory_threshold

           def get_throttle_factor(self) -> float:
               """Return 0.0-1.0 factor: 1.0 = no throttle, 0.0 = full throttle."""
               usage = self.get_usage()
               cpu_factor = max(0.0, 1.0 - max(0, usage["cpu_percent"] - self.cpu_threshold) / (100 - self.cpu_threshold))
               mem_factor = max(0.0, 1.0 - max(0, usage["memory_percent"] - self.memory_threshold) / (100 - self.memory_threshold))
               return min(cpu_factor, mem_factor)

    2. Create a module-level instance: `resource_monitor = ResourceMonitor()` in core/db/engine.py.

    3. In services/task_runtime.py _loop method: After calculating available_slots (line 61), apply resource-aware adjustment:
       ```python
       throttle = resource_monitor.get_throttle_factor()
       adjusted_slots = max(1, int(available_slots * throttle)) if throttle < 1.0 else available_slots
       ```
       Use adjusted_slots instead of available_slots for the dispatch loop.

    4. In application/tasks/task_runner.py _execute_register_task (line 343-344): The concurrency is already configurable via payload. No change needed — the resource throttling happens at TaskRuntime level (slot reduction), not at individual task level.

    5. Add a function `get_resource_metrics() -> dict` in core/db/engine.py that returns:
       - resource_monitor.get_usage() (cpu_percent, memory_percent)
       - pool status from engine.pool.status() if available (checkedin, checkedout, overflow)
       - throttle status: resource_monitor.should_throttle(), throttle_factor

    6. In TaskRuntime.get_runtime_stats() (from Plan 1 Task 1): Include resource metrics by calling get_resource_metrics().
  </action>
  <verify>
    <automated>cd /home/vitaly/projects/any-auto-register && python -c "
from core.db.engine import ResourceMonitor, resource_monitor, get_resource_metrics
rm = ResourceMonitor()
usage = rm.get_usage()
assert 'cpu_percent' in usage
assert 'memory_percent' in usage
assert not rm.should_throttle()  # default thresholds, should not throttle in normal conditions
factor = rm.get_throttle_factor()
assert 0.0 <= factor <= 1.0
metrics = get_resource_metrics()
assert 'cpu_percent' in metrics
assert 'memory_percent' in metrics
assert 'throttle_factor' in metrics
print('ResourceMonitor OK')
" && python -c "
from services.task_runtime import TaskRuntime
rt = TaskRuntime()
stats = rt.get_runtime_stats()
assert 'cpu_percent' in stats
assert 'throttle_factor' in stats
print('Runtime stats with resources OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class ResourceMonitor" core/db/engine.py returns 0
    - grep -q "resource_monitor" services/task_runtime.py returns 0 (imported and used)
    - grep -q "get_resource_metrics" core/db/engine.py returns 0
    - grep -q "throttle_factor" services/task_runtime.py returns 0
    - ResourceMonitor.get_usage() returns dict with cpu_percent and memory_percent
    - ResourceMonitor.should_throttle() returns bool based on thresholds
    - get_resource_metrics() returns combined resource + pool metrics
    - TaskRuntime._loop applies throttle factor to available_slots
  </acceptance_criteria>
  <done>ResourceMonitor tracks CPU/memory, TaskRuntime applies dynamic throttling when system is under load, resource metrics available via get_resource_metrics().</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| System resources → ResourceMonitor | CPU/memory readings are system-level, not user-controlled |
| Resource throttling → TaskRuntime slots | Throttle factor must not reduce slots below 1 (prevents deadlock) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03 | Denial of Service | ResourceMonitor | medium | mitigate | Throttle factor floored at 1 slot minimum; psutil import failure returns safe defaults (no throttle) |
| T-03-04 | Information Disclosure | get_resource_metrics | low | accept | Metrics expose only CPU/memory percentages and pool counts, no sensitive data; endpoint access controlled by existing auth |
| T-03-05 | Tampering | psutil import | low | accept | psutil is standard monitoring library; fallback to safe defaults if unavailable |
</threat_model>

<verification>
1. `python -c "from core.db.engine import ResourceMonitor; rm = ResourceMonitor(); assert rm.get_throttle_factor() <= 1.0; print('OK')"` — ResourceMonitor works
2. `python -c "from services.task_runtime import TaskRuntime; rt = TaskRuntime(); s = rt.get_runtime_stats(); assert 'throttle_factor' in s; print('OK')"` — Runtime stats include resources
3. ResourceMonitor.should_throttle() returns False under normal load
4. get_resource_metrics returns combined resource + pool data
</verification>

<success_criteria>
- ResourceMonitor class exists with get_usage(), should_throttle(), get_throttle_factor()
- TaskRuntime._loop applies resource throttle to available_slots (min 1 slot)
- get_resource_metrics() returns CPU, memory, pool status, and throttle factor
- TaskRuntime.get_runtime_stats() includes resource metrics
- System gracefully handles missing psutil (safe defaults, no crash)
- Worker pool dispatch remains priority-ordered under resource pressure
</success_criteria>

<output>
Create `.planning/phases/01-http-sessions/03-02-SUMMARY.md` when done
</output>
