---
phase: 01-v1-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/
  - static/
  - main.py
  - .gitignore
autonomous: true
requirements: [RM-01, RM-02]
must_haves:
  truths:
    - "Директория frontend/ (Vite+React) отсутствует в проекте"
    - "Директория static/ отсутствует в проекте"
    - "main.py не содержит код обслуживания static файлов и SPA fallback"
    - ".gitignore не содержит frontend/node_modules, frontend/dist"
  artifacts:
    - "main.py без static file mounting и SPA fallback"
    - ".gitignore без frontend/ записей"
  key_links:
    - "main.py FastAPI app без static mount — приложение не падает при отсутствии static/"
---

<objective>
Удалить старый frontend/ (Vite+React SPA) и static/ (build output) из проекта. Очистить main.py от кода обслуживания статических файлов и SPA fallback. Обновить .gitignore.

Purpose: Убрать dual-фронтенд — old SPA заменён на frontend-new/ (Next.js). static/ больше не существует, код его обслуживания — мёртвый.
Output: Очищенные main.py и .gitignore, удалённые frontend/ и static/ директории.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</execution_context>

<context>
@.planning/phases/01-v1-removal/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Удалить frontend/ и static/ директории, очистить main.py от static serving</name>
  <files>frontend/, static/, main.py</files>
  <action>
1. Удалить директорию frontend/ целиком: `rm -rf frontend/`
2. Удалить директорию static/ если существует: `rm -rf static/`
3. В main.py удалить строки 32-33 (импорты StaticFiles и FileResponse):
   - `from fastapi.responses import FileResponse`
   - `from fastapi.staticfiles import StaticFiles`
4. В main.py удалить блок строк 128-134 (static mount + SPA fallback):
   ```
   _static_dir = os.path.join(os.path.dirname(__file__), "static")
   if os.path.isdir(_static_dir):
       app.mount("/assets", StaticFiles(directory=os.path.join(_static_dir, "assets")), name="assets")

       @app.get("/{full_path:path}", include_in_schema=False)
       def spa_fallback(full_path: str):
           return FileResponse(os.path.join(_static_dir, "index.html"))
   ```
   Заменить на пустую строку или комментарий `# static/ and old SPA removed — frontend-new/ served separately`.
  </action>
  <verify>
    <automated>test ! -d frontend && test ! -d static && ! grep -q "StaticFiles\|spa_fallback\|_static_dir" main.py && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Директории frontend/ и static/ удалены. main.py не содержит импортов StaticFiles/FileResponse и блока static mount/SPA fallback.</done>
</task>

<task type="auto">
  <name>Task 2: Очистить .gitignore от frontend/ записей</name>
  <files>.gitignore</files>
  <action>
1. В .gitignore удалить секцию "# 前端" (строки 29-31):
   ```
   # 前端
   frontend/node_modules/
   frontend/dist/
   ```
   Заменить на комментарий `# old frontend removed — frontend-new/ uses its own .gitignore`.
  </action>
  <verify>
    <automated>! grep -q "frontend/node_modules\|frontend/dist" .gitignore && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>.gitignore не содержит записей для frontend/node_modules или frontend/dist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static serving removal | SPA fallback removed — no unauthenticated file serving |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information Disclosure | static/ directory | low | accept | static/ already absent, no data exposure risk |

</threat_model>

<verification>
1. `test ! -d frontend && test ! -d static` — директории удалены
2. `! grep -q "StaticFiles\|spa_fallback\|_static_dir" main.py` — main.py очищен
3. `! grep -q "frontend/node_modules\|frontend/dist" .gitignore` — .gitignore очищен
4. `python -c "from main import app; print('OK')"` — приложение импортируется без ошибок
</verification>

<success_criteria>
- frontend/ и static/ директории отсутствуют
- main.py не содержит static file serving кода
- .gitignore не содержит frontend/ записей
- Приложение импортируется без ошибок
</success_criteria>

<output>
Create `.planning/phases/01-v1-removal/01-01-SUMMARY.md` when done
</output>
