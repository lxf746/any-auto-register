---
phase: 07-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - .gitignore
  - Dockerfile
  - docker-compose.yml
  - main.py
autonomous: true
requirements: [CL-01, CL-02, CL-03]
---

<objective>
Verify and clean up infrastructure files (.gitignore, Dockerfile, docker-compose.yml) to ensure no legacy frontend/v1 references remain, and remove stale comments. Per CL-01, CL-02, CL-03.

Purpose: Final infrastructure polish — the project should have zero references to removed v1 artifacts in its deployment and version control config.
Output: Clean .gitignore, Dockerfile, docker-compose.yml with no legacy references; stale comments removed.
</objective>

<execution_context>
@/home/vitaly/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/vitaly/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-cleanup/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and clean .gitignore for legacy frontend entries (CL-01)</name>
  <files>.gitignore</files>
  <action>
    Audit .gitignore for any remaining v1/frontend references. Per CL-01: .gitignore must not contain frontend/node_modules or frontend/dist.

    Current state analysis:
    - Line 29: "# old frontend removed — frontend-new/ uses its own .gitignore" — this is a stale comment about the removed frontend, replace with cleaner note
    - No frontend/node_modules or frontend/dist entries exist (already clean)
    - Add a note that frontend-new/ has its own .gitignore if not already clear

    Clean up the comment block around line 29 to be concise and forward-looking, not retrospective about v1 removal.
  </action>
  <verify>
    <automated>cd /home/vitaly/projects/any-auto-register && ! grep -q "frontend/node_modules" .gitignore && ! grep -q "frontend/dist" .gitignore && ! grep -q "old frontend" .gitignore</automated>
  </verify>
  <done>.gitignore has no frontend/node_modules, frontend/dist entries; stale v1 removal comment replaced with concise note.</done>
</task>

<task type="auto">
  <name>Task 2: Clean Dockerfile and docker-compose.yml (CL-02, CL-03)</name>
  <files>Dockerfile, docker-compose.yml</files>
  <action>
    Dockerfile audit (CL-02: must not reference frontend/ directory):
    - Line 1 comment "# Stage 1: Build frontend" — keep (still builds frontend-new)
    - Line 46: `RUN rm -rf .venv frontend frontend-new` — remove `frontend` from this rm (the directory no longer exists, the rm is harmless but misleading). Change to `RUN rm -rf .venv frontend-new`
    - All COPY statements correctly reference frontend-new/ — no changes needed

    docker-compose.yml audit (CL-03: must not contain legacy services):
    - Current file is clean: only `app` and `postgres` services
    - No legacy services to remove
    - No changes needed

    After Dockerfile edit, verify the file is syntactically correct.
  </action>
  <verify>
    <automated>cd /home/vitaly/projects/any-auto-register && ! grep -q "rm -rf.*frontend[^-]" Dockerfile && ! grep -q "COPY frontend/" Dockerfile && docker compose config --quiet 2>/dev/null || echo "docker compose not available, skipping syntax check"</automated>
  </verify>
  <done>Dockerfile no longer references non-existent frontend/ directory in rm commands; docker-compose.yml confirmed clean; all tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docker build | Dockerfile changes affect container build — must still produce working image |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-03 | Elevation of Privilege | Dockerfile rm change | low | mitigate | Removing non-existent dir from rm is safe; existing tests verify build |
| T-07-04 | Tampering | docker-compose.yml | low | accept | No changes to docker-compose.yml — file already clean |
| T-07-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this phase |
</threat_model>

<verification>
- `.gitignore` contains no `frontend/node_modules` or `frontend/dist`
- `Dockerfile` does not reference `frontend/` directory (only `frontend-new/`)
- `docker-compose.yml` has no legacy services
- `pytest tests/ -x --tb=short` — all tests pass
</verification>

<success_criteria>
- .gitignore clean of v1 frontend entries
- Dockerfile references only frontend-new/ (not frontend/)
- docker-compose.yml contains only app + postgres services
- No stale v1 comments in infrastructure files
- All tests pass
</success_criteria>

<output>
Create `.planning/phases/07-cleanup/07-02-SUMMARY.md` when done
</output>
