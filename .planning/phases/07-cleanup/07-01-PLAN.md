---
phase: 07-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/base_mailbox.py
  - core/generic_http_mailbox.py
  - core/local_ms_mailbox.py
  - platforms/fireworks/plugin.py
  - platforms/kimchi/plugin.py
  - platforms/cerebras/plugin.py
  - platforms/trae/plugin.py
  - platforms/windsurf/plugin.py
  - platforms/kiro/plugin.py
  - platforms/grok/plugin.py
  - platforms/openblocklabs/plugin.py
  - platforms/cursor/plugin.py
  - platforms/tavily/plugin.py
  - platforms/blackbox/plugin.py
  - platforms/blink/plugin.py
  - platforms/chatgpt/plugin.py
  - platforms/anything/plugin.py
  - application/tasks/task_runner.py
  - main.py
  - api/v2/router.py
  - api/v2/auth.py
autonomous: true
requirements: [CL-01, CL-02, CL-04]
---

<objective>
Replace all deprecated core.base_mailbox imports with core.mailbox imports and clean up v1/v2 transition comments across the codebase. Per CL-04: deprecated functions and legacy code removed from codebase.

Purpose: Remove the backward-compatibility shim layer that was left after v1 removal, ensuring the codebase has no deprecated import paths.
Output: All 18 files importing from core.base_mailbox now import directly from core.mailbox; core/base_mailbox.py deleted or gutted; v1 transition comments cleaned.
</objective>

<execution_context>
@/home/vitaly/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/vitaly/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace deprecated core.base_mailbox imports with core.mailbox</name>
  <files>
    platforms/fireworks/plugin.py,
    platforms/kimchi/plugin.py,
    platforms/cerebras/plugin.py,
    platforms/trae/plugin.py,
    platforms/windsurf/plugin.py,
    platforms/kiro/plugin.py,
    platforms/grok/plugin.py,
    platforms/openblocklabs/plugin.py,
    platforms/cursor/plugin.py,
    platforms/tavily/plugin.py,
    platforms/blackbox/plugin.py,
    platforms/blink/plugin.py,
    platforms/chatgpt/plugin.py,
    platforms/anything/plugin.py,
    core/generic_http_mailbox.py,
    core/local_ms_mailbox.py,
    application/tasks/task_runner.py
  </files>
  <behavior>
    - Test: All imports from core.base_mailbox resolve to core.mailbox
    - Test: No file contains "from core.base_mailbox import" after migration
    - Test: All existing tests pass (pytest tests/ -x)
  </behavior>
  <action>
    Replace all `from core.base_mailbox import ...` statements with direct `from core.mailbox import ...` across 17 files. Per CL-04.

    Migration map (core.base_mailbox re-exported these names):
    - `BaseMailbox` → `from core.mailbox.base import BaseMailbox`
    - `MailboxAccount` → `from core.mailbox.models import MailboxAccount`
    - `create_mailbox` → `from core.mailbox.registry import create_mailbox`
    - `_extract_verification_link` → `from core.mailbox.utils import extract_verification_link` (also rename alias: `_extract_verification_link` → `extract_verification_link`)
    - `_normalize_api_base_url` → `from core.mailbox.utils import normalize_api_base_url` (also rename alias)
    - `FallbackMailbox` → `from core.mailbox.base import FallbackMailbox`

    For each file, determine which names it imports and update to the direct path. Example:

    Old: `from core.base_mailbox import BaseMailbox`
    New: `from core.mailbox.base import BaseMailbox`

    Old: `from core.base_mailbox import BaseMailbox, MailboxAccount, _extract_verification_link`
    New: `from core.mailbox.base import BaseMailbox` + `from core.mailbox.models import MailboxAccount` + `from core.mailbox.utils import extract_verification_link as _extract_verification_link`

    IMPORTANT: In core/generic_http_mailbox.py and core/local_ms_mailbox.py, the alias `_extract_verification_link` is used internally. Keep the `as _extract_verification_link` alias so internal references don't break.

    After updating all imports, run `pytest tests/ -x --tb=short` to verify nothing breaks.
  </action>
  <verify>
    <automated>cd /home/vitaly/projects/any-auto-register && grep -r "from core.base_mailbox import" --include="*.py" | grep -v base_mailbox.py | wc -l | xargs -I{} test {} -eq 0 && pytest tests/ -x --tb=short -q</automated>
  </verify>
  <done>All 17 files import directly from core.mailbox submodules; zero files (except base_mailbox.py itself) reference core.base_mailbox; all tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Delete deprecated base_mailbox shim and clean up v1 transition comments</name>
  <files>
    core/base_mailbox.py,
    main.py,
    api/v2/router.py,
    api/v2/auth.py
  </files>
  <action>
    Delete core/base_mailbox.py entirely — it was a backward-compatibility shim that re-exported from core.mailbox. All imports now go directly to core.mailbox submodules (Task 1). Per CL-04.

    Clean up v1/v2 transition comments that are no longer relevant:
    - main.py line 32: Remove comment "# static/ and old SPA removed — frontend-new/ served separately" (the migration is long done)
    - api/v2/router.py line 61: Change comment "# Rate limiting (reuse v1 logic)" to just "# Rate limiting" (v1 is gone, this IS the only logic now)
    - api/v2/auth.py line 3: Remove docstring line "Moved from api/auth.py to decouple v2 from v1 modules." — just describe what the module does, not where it came from

    After cleanup, run `pytest tests/ -x --tb=short` to verify nothing breaks.
  </action>
  <verify>
    <automated>cd /home/vitaly/projects/any-auto-register && test ! -f core/base_mailbox.py && ! grep -q "reuse v1 logic" api/v2/router.py && ! grep -q "Moved from api/auth.py" api/v2/auth.py && pytest tests/ -x --tb=short -q</automated>
  </verify>
  <done>core/base_mailbox.py deleted; v1 transition comments removed from main.py, api/v2/router.py, api/v2/auth.py; all tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| import chain | Replacing deprecated shim imports could break runtime if any file was missed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | core/base_mailbox.py deletion | medium | mitigate | Grep verification ensures zero remaining imports before deletion |
| T-07-02 | Info Disclosure | v1 transition comments | low | accept | Comments contain no sensitive data, only migration notes |
| T-07-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this phase |
</threat_model>

<verification>
- `grep -r "from core.base_mailbox import" --include="*.py" | grep -v base_mailbox.py` returns empty (no remaining deprecated imports)
- `test ! -f core/base_mailbox.py` confirms shim deleted
- `pytest tests/ -x --tb=short` — all tests pass
- No v1 transition comments remain in main.py, api/v2/router.py, api/v2/auth.py
</verification>

<success_criteria>
- Zero files import from core.base_mailbox (except the deleted shim)
- core/base_mailbox.py does not exist
- No v1/v2 migration comments remain in production code
- All tests pass
</success_criteria>

<output>
Create `.planning/phases/07-cleanup/07-01-SUMMARY.md` when done
</output>
